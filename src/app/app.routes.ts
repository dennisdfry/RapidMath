import { Routes } from '@angular/router';
import { MathematicsComponent } from './mathematics/mathematics.component';

export const routes: Routes = [
    {path:'', component: MathematicsComponent}
];
